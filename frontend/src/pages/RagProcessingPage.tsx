export function RagProcessingPage() {
  return (
    <div className="animate-fade-in" style={{ padding: 40, textAlign: 'center', color: 'var(--text-muted)' }}>
      <h2>資料庫與處理平台</h2>
      <p>模組邊界已預留，後續會在獨立知識處理模組中落地，不再與 crawler 核心混寫。</p>
    </div>
  );
}